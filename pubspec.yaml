name: ai_wellness_coach_app
description: "A health and wellness coach app powered by Firebase AI."
publish_to: 'none'
version: 1.0.0+1

environment:
  sdk: ^3.11.4

dependencies:
  flutter:
    sdk: flutter
  cupertino_icons: ^1.0.8
  equatable: ^2.0.7
  firebase_ai: ^3.9.0
  firebase_core: ^4.2.0
  firebase_remote_config: ^6.1.0
  flutter_bloc: ^9.1.1
  intl: ^0.20.2
  shared_preferences: ^2.5.3

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^6.0.0

flutter:
  uses-material-design: true
